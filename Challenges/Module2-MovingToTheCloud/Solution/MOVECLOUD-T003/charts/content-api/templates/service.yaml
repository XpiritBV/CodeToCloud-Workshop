apiVersion: v1
kind: Service
metadata:
  name: {{ include "content-api.fullname" . }}
  labels:
    {{- include "content-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    {{- include "content-api.selectorLabels" . | nindent 4 }}
